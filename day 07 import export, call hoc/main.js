// import -> default, name, export -> default, name, alias
import {default as addFunc} from "./calculation";

const subResult = calc.multi();
const multi = calc.subtractionFunction()

const result = addFun(2, 3);
console.log(result);